<% item.comments.each do |comment| %>
<div class="card">
    <div class="card-body">
        <div class="card-text">
            <p><%= comment.comment %></p>
            <p><%= comment.user.name %></p>
            <%# if comment.user == current_user %>
                
        </div>
        
        
    </div>
</div>
<% end %>
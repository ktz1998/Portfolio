<div class="container">
  <h2 class="navbar">気になる製品を見てみよう</h2>
  
  <%= render "layouts/tag", tags: @tags %>  <!--タグ一覧表示-->
  <div class="row">
    <%= render "index", items: @items %>
  </div>
  <%= paginate @items %>
</div>
